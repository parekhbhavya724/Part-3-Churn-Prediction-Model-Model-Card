# Part 3 - Churn Prediction Model

```bash
python -m pip install -r requirements.txt
jupyter notebook churn_model.ipynb
```

Generated outputs include `model.pkl`, `metrics.json`, `error_analysis.md`, `model_card.md`, and `feature_importance.csv`.
